// Package services provides core business logic.
package services

// InitializeServices sets up all service dependencies
func InitializeServices() {
	// Services are initialized lazily via their getter functions
	// This ensures proper singleton pattern and dependency injection
}
